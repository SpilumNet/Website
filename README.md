Copy **example-config.json** to **config.json** and input the credentials to get started.

Install requirements with Python 3.7 using the command `pip install -r requirements.txt`

Run application with `flask run`